import { AssetManager } from "@/components/asset-manager";

export default function CollectionPage() {
  return <main className="placeholder-page collection-page"><p className="eyebrow">Private collection</p><h1 className="section-title">Your<br /><em>collection.</em></h1><p>Add and curate the objects, places, and pieces that define your world.</p><AssetManager /></main>;
}
